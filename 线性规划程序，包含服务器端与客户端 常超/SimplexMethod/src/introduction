/**

         * 首先我们要读取的信息分为：、
         * 1.c，即需要极小化的系数
         * 2.Aeq，即等于等式约束部分的系数矩阵
         * 3.beq，即等式约束部分的约束结果
         * 4.Aleq，即不等式部分小于的系数矩阵
         * 5.bleq，不等式小于的约束结果
         * 大于部分可以视情况而定，可以直接系数乘以-1换成小于
         * 6.Ageq，大于部分的系数矩阵
         * 7.bgeq，大于部分的约束结果
         * 8.未知量小于0的约束
         * 9.无约束指标

         本程序主要为通过TCP/IP协议面向连接，通过连接套接字传递string，再将string解读为ToStandard类的构造所需要的东西，最后将
         计算的结果转化为String返回服务器类并发送到客户端。该服务器程序利用线程池，支持多用户独立访问连接。

         该程序有个问题就是它返回的结果是转化为标准型问题的结果。这个问题很容易解决，但我不想解决了，反正一眼就能从这儿的答案看出
         原文题的答案，因为麻烦的无约束项已经去掉了，只需要自己处理负约束项和人工变量即可。一般直接看约束变量有没有在无约束指标集
         里，有的话直接变为负的。然后取对应的变量即可。

         useless文件为一些无用的代码，担心还可能有用，暂存在该文件中。
